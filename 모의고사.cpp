#include <iostream>
#include <vector>

using namespace std;

vector<int> solution(vector<int> answers)
{
	vector<int> answer;


	return answer;
}

int main()
{
	int max;
	int input;
	vector<int> answers;
	vector<int> 
	cin >> max;

	for(int i = 0 ; i < max; i++)
	{
		cin >> input;
		answers.push_back(input);
	}




	return 0;
}